"use strict";
// CPU chooses worst result
var emResultPowerMin = Math.min(emStar, emHeart, emDefence);
var emResultTwistMin = Math.min(emBlue, emOrange, emGreen);
if (emStar = emResultPowerMin) {
    emPower = emResultPowerMin;
    console.log(({ emPower: emPower }));
}
else if (emHeart = emResultPowerMin) {
    emPower = emResultPowerMin;
    console.log(({ emPower: emPower }));
}
else if (emDefence = emResultPowerMin) {
    emPower = emResultPowerMin;
    console.log(({ emPower: emPower }));
}
//Find which twist was the greater and add it's value to emTwist
if (emBlue = emResultTwistMin) {
    emTwist = emResultTwistMin;
    console.log(({ emTwist: emTwist }));
}
else if (plOrange = emResultTwistMin) {
    emTwist = emResultTwistMin;
    console.log(({ emTwist: emTwist }));
}
else if (emGreen = emResultTwistMin) {
    emTwist = emResultTwistMin;
    console.log(({ emTwist: emTwist }));
}
console.log(("CPU Is Dumb"));
