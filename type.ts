let star = 1
let heart = 8
let defence = 3
let blue = 6
let orange =1
let green =4

let resultPower = Math.max(star,heart,defence)
let resultTwist = Math.max(blue,orange,green)

if 
(star = resultPower )
{
    let emPower = resultPower
   console.log(( {emPower} ) );
}

else if
(heart = resultPower) {
    let emPower = resultPower
   console.log(( {emPower} ) );
}    

else if 
(defence = resultPower) {
    let emTwist = resultPower
    console.log(( {emTwist} ) );
}    

if 
(star = resultTwist )
{
    let emTwist = resultTwist
   console.log(( {emTwist} ) );
}

else if
(heart = resultTwist) {
    let emTwist = resultTwist
   console.log(( {emTwist} ) );
}    

else if 
(defence = resultTwist) {
    let emTwist = resultTwist
    console.log(( {emTwist} ) );
}    



console.log(( {resultPower} ) );
console.log(( {resultTwist} ) );
